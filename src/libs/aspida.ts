// APIの型定義が完了したら、以下のコメントアウトを外す

/*
import aspidaAxios from '@aspida/axios';

import api from '../api/$api';

import { client } from './axios';

const aspida = api(aspidaAxios(client));

export default aspida;
 */
